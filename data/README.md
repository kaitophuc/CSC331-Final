# Data Directory

Expected layout for a local YOLO dataset:

```
data/
  dataset.yaml
  train/
    images/
    labels/
  val/
    images/
    labels/
  infer/
    image1.jpg
    image2.jpg
```

`dataset.yaml` should point to your train/val image folders and class names.

